rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    match /users/{userId}/photos/{imageId} {
      // Only the authenticated user can read or write their own photos
      allow read, write: if request.auth != null && request.auth.uid == userId;
    }
  }
}
